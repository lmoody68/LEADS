# Python
**/__pycache__/
*.py[cod]
**/.venv/
**/venv/
**/*.egg-info/

# Environment / secrets
**/.env

# ChromaDB persistent store (local legal corpus + privileged uploads — never publish)
**/chroma_db/
**/uploads/

# CourtListener opinion cache + server logs (local only)
**/.cache/
**/server.log
**/*.log

# BKT per-session mastery state (Phase 4) — local only, anonymous, never published
**/bkt_state/

# Phase 7 ingestion: registered public-dataset metadata (local aux store, never published)
**/datasets_store/

# Node / Vite
**/node_modules/
**/dist/

# OS
.DS_Store
Thumbs.db

# Trained ML model artifacts (retrainable from the corpus)
backend/models/
