fastapi
uvicorn[standard]
chromadb
rank_bm25
pypdf
pdfplumber
python-multipart
httpx
python-dotenv
# Phase 7 — public legal-dataset discovery (HF Hub listing API; no auth needed).
huggingface_hub
# Phase 8 — cross-encoder reranker (ONNX via onnxruntime; NO torch). Graceful:
# the app still runs if this is absent (retrieval falls back to RRF order).
fastembed
# Phase 8 — LangSmith LLM observability (OPTIONAL; tracing only when
# LANGSMITH_API_KEY is set). The router degrades to a no-op if this is absent.
langsmith
# Phase 8 — auxiliary document classifier (supervised-ML showcase): sklearn
# LogisticRegression head on the corpus's ONNX MiniLM embeddings (NO torch).
# Graceful: the classifier returns a clean error if these are absent.
scikit-learn
joblib
# MCP server (backend/mcp_server.py) — exposes the app as MCP tools for Claude
# Desktop / Claude Code. Optional; only needed to run the MCP wrapper.
mcp[cli]
# PDF export of memos / case briefs (pure-Python, no system libs).
fpdf2
# OPTIONAL Phase 7 deps — lazy/guarded imports; the app boots fine WITHOUT them.
#   datasets : stream a small sample of a public legal dataset into the corpus.
#   kaggle   : discover Kaggle legal datasets (only if kaggle.json creds present).
# Uncomment to enable:
# datasets
# kaggle — ENABLED: Kaggle public-dataset discovery (official API; KAGGLE_API_TOKEN
# or kaggle.json creds; gracefully skipped if absent).
kaggle
# OCR is OPTIONAL and import-guarded in code (requires a Tesseract binary on PATH).
# Uncomment to enable image OCR for the Document Analyzer:
# pytesseract
# Pillow
