# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/

# Environment / secrets
.env

# ChromaDB persistent store (local legal corpus + uploaded privileged docs — never publish)
chroma_db/

# Uploaded case files staging (privileged — local only)
uploads/

# CourtListener opinion cache (locally fetched public opinions — kept local to
# respect rate limits; never published)
.cache/

# BKT per-session mastery state (Phase 4) — anonymous probability vectors keyed
# by session id; local only, never published. Also covered by the repo-root
# .gitignore; duplicated here as defense-in-depth.
bkt_state/

# Server logs
server.log
*.log

# OS
.DS_Store
Thumbs.db
